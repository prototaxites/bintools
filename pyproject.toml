[project]
name = "bin-tools"
version = "0.1.0"
description = "A toolkit for collating and manipulating metagenome bins."
readme = "README.md"
authors = [
    { name = "Jim Downie", email = "jd42@sanger.ac.uk" }
]
license = "MIT"
license-files = ["LICEN[CS]E*"]
requires-python = ">=3.13"
dependencies = [
    "click>=8.5.0",
    "loguru>=0.7.3",
    "pydantic>=2.13.5",
    "pyfastx>=2.3.1",
    "zstandard>=0.25.0",
]

[project.scripts]
bintools = "bin_tools.cli.cli:cli"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "sphinx>=9.1.0",
    "sphinx-click>=6.2.0",
    "sphinx-rtd-theme>=3.1.0",
]
